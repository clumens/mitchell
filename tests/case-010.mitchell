# Description:  Aliased type for branch-expr is still the same type
# Expected:  PASS

ℳ Main ← decl
   τ bool ← boolean

   ʋ val: integer ← 47
   ʋ result: bool ← f

   ƒ foo: boolean () ←
      case val in
         10 → t,
         else → result
      end
end
