# Description:  case expression with just a default
# Expected:  PASS

ℳ Main ← decl
   ƒ asdf()(): string ← "asdf"

   ƒ foo()(): boolean ←
      case Main.asdf()() in
         else → f
      end
end
