# Description:  Declaration as part of an if-then-else
# Expected:  PASS

ℳ Main ← decl
   ƒ foo: integer ()
in
   ƒ foo: integer () ←
      if decl ʋ x: boolean ← true in x end then 47 else X.Y.Z (0)
end
