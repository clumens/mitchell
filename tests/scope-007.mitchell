# Description:  Function tries to return a type it defines itself.
# Expected:  FAIL
# HowFail:  some_ty: Referenced symbol is unknown.

ℳ Main ← decl
   ƒ foo()(): some_ty ← decl
      τ some_ty ← boolean
   in
      t
   end
end
