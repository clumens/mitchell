# Description:  Use of inferred type does not match actual type
# Expected:  FAIL
# HowFail:  Type check error: if expression test must return a boolean type.

ℳ Main ← decl
   ʋ x ← 47
   ʋ y: boolean ← if x then t else f
end
