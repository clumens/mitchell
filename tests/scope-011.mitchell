# Description:  A group of recursive types broken up by a function.
# Expected:  FAIL
# HowFail:  data: Referenced symbol is unknown.

ℳ Main ← decl
   τ node ← { data: data, left: node, right: node }

   ƒ fun()(): integer ← 47

   τ data ← { name: string, age: integer }
end
