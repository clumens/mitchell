# Description:  Calculate the Fibonacci number using slow, simple recursion
# Expected:  PASS
# Disabled

ℳ Main ← decl
   ƒ fib()(n: integer): integer ←
      if Boolean.or()(Integer.=()(n, 0), Integer.=()(n, 1)) then 1
      else Integer.+()(fib()(Integer.-()(n, 1)), fib()(Integer.-()(n, 2)))
end
