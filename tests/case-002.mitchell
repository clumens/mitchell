# Description:  Basic branch-expr possibilities
# Expected:  FAIL

ℳ Main ← decl
   ƒ asdf: string () ← "asdf"

   ƒ foo: integer () ←
      case Main.asdf() in
         X.Y → 1,
         "one" → 1,
         0 → 0,
         f → 0
      end
end
