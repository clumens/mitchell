# Description:  Basic branch-expr possibilities
# Expected:  PASS

ℳ Main ← decl
   ƒ foo: integer ()
in
   ƒ foo: integer () ←
      case Main.asdf() in
         X.Y → 1,
         "one" → 1,
         0 → 0,
         f → 0
      end
end
