# Description:  Basic branch-expr possibilities
# Expected:  PASS

ℳ Main ← decl
   ƒ foo: int ()
in
   ƒ foo: int () ←
      case Main.asdf() in
         X.Y → 1,
         "one" → 1,
         0 → 0,
         false → 0
      end
end
