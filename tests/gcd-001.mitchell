# Description:  Finding the GCD of two numbers via Euclid's algorithm
# Expected:  PASS

ℳ Main ← decl
   ƒ gcd: integer (a: integer, b: integer) ←
      if Integer.= (b, 0) then a
      else gcd (b, Integer.mod (a, b))
end
