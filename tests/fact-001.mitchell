# Description:  Factorial computation
# Expected:  PASS

ℳ Main ← decl
   ƒ fact: integer (n: int)
in
   ƒ fact: integer (n: int) ←
      if Integer.= (n, 0) then
         1
      else
         Integer.* (n, fact (Integer.- (n, 1)))
end
