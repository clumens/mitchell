# Description:  Factorial computation
# Expected:  PASS

ℳ Main ← decl
   ƒ fact: int (n: int)
in
   ƒ fact: int (n: int) ←
      if Boolean.= (n, 0) then
         1
      else
         Integer.* (n, fact (Integer.- (n, 1)))
end
