# Description:  Basic type definitions
# Expected:  PASS

ℳ Main ← decl
   τ uid ← integer
   τ name ← string
   τ is_root ← boolean
   τ things ← string list
in
   τ uid ← integer
   τ name ← string
   τ is_root ← boolean
   τ things ← string list
end
